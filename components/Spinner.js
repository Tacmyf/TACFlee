import { GridLoader } from "react-spinners";

const Spinner = () => (
  <div className="loader">
    <div className="cube">
      <GridLoader color="#304FFF" />
    </div>
  </div>
);

export default Spinner;
